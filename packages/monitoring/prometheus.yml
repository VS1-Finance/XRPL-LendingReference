global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # The history store's derived metrics, exposed by postgres_exporter.
  - job_name: lending-history-store
    static_configs:
      - targets: ["postgres-exporter:9187"]
